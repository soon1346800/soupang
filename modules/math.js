exports.add = function (a, b) {
    return a + b;
}


// function add(a, b) {
//     return a + b;
// }

// 모듈 그 자체로 바로 add 함수를 할당한다.
// module.exports = add;

// 모듈을 호출했을 때, add 키 값에는 add 함수가 들어가는 방법이다.
// module.exports = { add : add};